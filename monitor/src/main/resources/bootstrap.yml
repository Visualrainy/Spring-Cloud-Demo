spring:
  application:
    name: monitor

eureka:
  instance:
    non-secure-port: ${server.port:8989}
  client:
    service-url:
      defaultZone: http://${eureka.host:locahost}:${eureka.port:8761}/eureka/